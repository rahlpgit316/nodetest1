extends layout

block content
    h1= title
    p Welcome to our test
    div(ng-controller='userCtrl')
      // Wrapper
      #wrapper

          // USER LISTs
          h2 User List
          #userList
              table
                  thead
                      th UserName
                      th Email
                      th Delete?
                  tbody
                    tr(ng-repeat='user in userlist')
                      td {{user.username}}
                      td {{user.email}}
                      td 
                        a(href='#') {{user._id}}
          // /USER LIST

      // /Wrapper

